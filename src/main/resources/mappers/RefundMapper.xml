<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baby.babycareproductsshop.order.refund.refundMapper">
    <insert id="InsRefund" useGeneratedKeys="true" keyProperty="t_refund">
        INSERT INTO t_refund
        SET idetails = #{idetails},
        completefl = #{completefl},
        created_at = #{createdAt}
    </insert>

   <!-- <select id="AfterRefund">

    </select> -->
</mapper>